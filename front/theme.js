import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
  fonts: {
    heading: `'Lilita One', sans-serif`, // Just a font-family string
    body: `'Poppins', sans-serif`,
  },
});

export default theme;
